# Evidence: checkpoint compression

Date: 2026-04-30
Worktree: /Users/fukujusou/workspace/openchat
Branch: main

## Static propagation surface

Commands:

```
git status --short --branch
find OpenChat -name '*.swift' | wc -l
find OpenChatTests -name '*.swift' | wc -l
find OpenChat/Core/ContextManager -name '*.swift' | wc -l
find OpenChat/Core/Database -name '*.swift' | wc -l
rg -n "CompressionCheckpoint|CheckpointCompactor|CompressionPolicy|CompressionMode|PreparedHistory|compressionMode|deleteCompressionCheckpoints|conversation_compression_checkpoint|autoCompactTokenLimit|prepareContextHistory" OpenChat OpenChatTests arch .github -g '!OpenChat/Resources/Models/**'
```

Observed counts after compression mode work:

```
App Swift files: 106
Test Swift files: 31
Context Swift files: 11
Database Swift files: 19
```

Key propagation facts:

- New production types are confined to `OpenChat/Core/Database` and `OpenChat/Core/ContextManager`; the UI propagation point is `ChatSettingsSheet` plus `ChatViewModel.selectedCompressionMode`.
- `OpenChat/Features/Chat/ViewModels/ChatViewModel.swift` persists `compressionMode` and invalidates checkpoint cache after edit/delete history operations.
- `CheckpointCompactor` rejects checkpoints whose saved `effectiveCompactWindowTokens` / `autoCompactTokenLimit` do not match the current mode policy.
- No new Feature-to-Feature dependency was introduced by checkpoint compression.
- No Shared-to-Core dependency was introduced by checkpoint compression.

## Focused verification

Context:

```
xcodebuild test -project OpenChat.xcodeproj -scheme OpenChat -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -only-testing:OpenChatTests/CompressionPolicyTests -only-testing:OpenChatTests/CompressionSourceHasherTests -only-testing:OpenChatTests/PreparedHistoryTests -only-testing:OpenChatTests/CompressionSummarizerTests -only-testing:OpenChatTests/CheckpointCompactorTests -only-testing:OpenChatTests/CompressionCheckpointReuseTests
```

Result:

```
Test run with 14 tests in 6 suites passed
** TEST SUCCEEDED **
```

Database:

```
xcodebuild test -project OpenChat.xcodeproj -scheme OpenChat -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -only-testing:OpenChatTests/MigrationTests -only-testing:OpenChatTests/CompressionCheckpointDatabaseTests
```

Result:

```
Test run with 24 tests in 2 suites passed
** TEST SUCCEEDED **
```

Chat/prompt:

```
xcodebuild test -project OpenChat.xcodeproj -scheme OpenChat -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -only-testing:OpenChatTests/ChatViewModelPromptAssemblyTests -only-testing:OpenChatTests/PromptAssemblerTests
```

Result:

```
Test run with 16 tests in 2 suites passed
** TEST SUCCEEDED **
```

Compression mode focused:

```
xcodebuild test -project OpenChat.xcodeproj -scheme OpenChat -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -only-testing:OpenChatTests/CompressionPolicyTests -only-testing:OpenChatTests/CompressionCheckpointReuseTests -only-testing:OpenChatTests/MigrationTests -only-testing:OpenChatTests/ChatViewModelPromptAssemblyTests
```

Result:

```
Test run with 39 tests in 4 suites passed
** TEST SUCCEEDED **
```

## Full verification

Command:

```
xcodebuild test -project OpenChat.xcodeproj -scheme OpenChat -destination 'platform=iOS Simulator,name=iPhone 17 Pro'
```

Result:

```
Test run with 192 tests in 41 suites passed
** TEST SUCCEEDED **
```

## Propagation audit writeback

- `arch/AntiEntropy/propagation-audit.md` has `2026-04-30 Checkpoint Compression Incremental Audit` and `2026-04-30 Compression Mode Threshold Incremental Audit` sections.
- `arch/AntiEntropy/triangle-consistency.md` records checkpoint compression arch-src / arch-test / src-test alignment.
- `arch/AntiEntropy/index.md` now points to this harness folder and the 192-test current baseline.
