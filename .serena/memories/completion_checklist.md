After implementation work, verify the affected roadmap phase checklist, keep code and arch docs synchronized, and add implementation evidence back into the docs per AGENTS.md. Run targeted tests with Swift Testing, especially for Core modules: TokenCounter, PromptAssembler, KeywordMatcher, SSEStreamParser, ContextManager strategies, world book import parsing, and database migrations. Avoid mutating existing migration history; add new migrations only. Confirm dependency direction still matches App -> Features -> Core -> Shared.