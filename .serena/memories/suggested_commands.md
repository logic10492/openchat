Current repo has no Xcode project yet, so build/test commands will become valid after bootstrap. Environment checks: xcodebuild -version -> Xcode 26.4 (17E192); swift --version -> Apple Swift 6.3. Useful shell commands on this Darwin system: ls, pwd, rg --files, rg <pattern>, sed -n 'start,endp' <file>. Expected implementation-time commands after project bootstrap: xcodebuild -scheme OpenChat -destination 'platform=iOS Simulator,name=iPhone 16' build; xcodebuild -scheme OpenChat -destination 'platform=iOS Simulator,name=iPhone 16' test. The workspace path is /Volumes/SN550-Work/workspace/openchat.